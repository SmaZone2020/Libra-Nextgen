[package]
name = "forkexec-module"
version = "0.1.0"
edition = "2021"

[lib]
name = "forkexec_module"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = { workspace = true }
libra-platform = { path = "../../libra-platform" }
