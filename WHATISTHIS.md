# Libra-Nextgen 产品描述

> 企业级红蓝对抗 C2 框架 | 多平台后渗透 | 多人协同作战

---

## 一、产品概述

Libra-Nextgen 是一款专为现代企业级网络安全红蓝对抗设计的高性能、高隐蔽性 C2（Command and Control）框架。系统采用经典的三层分离架构，提供从终端植入、远程控制到数据窃取的全方位后渗透能力，支持多操作员实时协同作战。

**项目地址：** https://github.com/SmaZone2020/Libra-Nextgen

### 技术架构

```
┌──────────────────┐    WebSocket / REST     ┌──────────────────┐
│  Libra-Console   │ ◄─────────────────────► │  Libra-Server    │
│  (React Web UI)  │   JWT 认证 + RBAC       │  (ASP.NET Core)  │
│  多人协同控制台   │   实时状态同步           │  MongoDB 持久化   │
└──────────────────┘                         └────────┬─────────┘
                                                      │
                                         AES-256-GCM  │  WebSocket
                                         + RSA 协商   │  / HTTP(S)
                                                      │
                                           ┌──────────┴──────────┐
                                           │  Libra-Agent         │
                                           │  (.NET Native AOT)   │
                                           │  内存驻留 模块化载荷  │
                                           └─────────────────────┘
```

### 三层架构

| 层级 | 技术栈 | 职责 |
|------|--------|------|
| **Libra-Agent (被控端)** | .NET 10 Native AOT | 轻量级植入体，内存驻留，按需加载后渗透模块 |
| **Libra-Server (服务端)** | ASP.NET Core WebAPI + WebSocket | 流量接入、任务调度、数据持久化、多控制台消息广播 |
| **Libra-Console (控制端)** | React + Vite + HeroUI + TailwindCSS | 多人协同 Web 操作平台，实时交互终端，数据可视化 |

---

## 二、核心安全机制

### 2.1 通信加密

Agent 与 Server 之间的所有通信强制采用 **AES-256-GCM** 加密，密钥在 Agent 首次上线时通过 **RSA 非对称加密**进行协商，每个 Agent 拥有独立的会话密钥，防止流量被中间人抓包分析。

### 2.2 流量伪装（Malleable C2）

Server 端支持动态配置通信 Profile 文件，可自定义 HTTP 请求特征：
- 将 C2 通信 URL 伪装成正常业务 API 路径
- 将数据载荷隐藏在 HTTP Header、Cookie 中
- 支持动态切换 User-Agent 列表
- 流量特征与正常业务高度一致，绕过 NDR/WAF 检测

### 2.3 控制台安全

- **JWT 认证**：采用 RS256 非对称签名，防止令牌伪造
- **RBAC 权限模型**：区分 `Operator`（操作员）和 `Admin`（管理员），防止越权操作
- **首次启动引导**：系统首次运行强制引导创建管理员账户，无默认密码
- **强制审计**：所有通过 Console 下发的指令全部写入 `AuditLogs` 集合，前端不提供删除日志的权限

### 2.4 反检测与隐蔽

- **Native AOT 编译**：Agent 编译为原生机器码，剥离 .NET 运行时依赖，消除 JIT/IL 元数据特征
- **内存驻留**：所有高级后渗透模块通过加密信道从 Server 下载，使用 `Assembly.Load()` 在内存中执行，不落盘
- **反沙盒/反虚拟机**：检测 VMware/VirtualBox/Hyper-V 特征（MAC 地址前缀、注册表键值、驱动文件）；校验 CPU 核心数、内存大小、鼠标移动轨迹等环境探针
- **一键自毁**：赛事结束后，Server 向所有在线 Agent 下发清理指令，自动撤销持久化项，释放内存并终止进程

---

## 三、功能详解

### 3.1 仪表盘（Dashboard）

操作员登录后的首页总览，提供全局态势感知：

- **Agent 在线统计**：在线/离线/总数实时计数
- **任务统计**：任务总数、等待中、活跃中、已完成
- **流量监控**：按时间范围（14天/7天/3天/今日/12小时）展示总流量趋势
- **全球设备分布图**：结合 GeoIP 数据在交互式地图上标记所有 Agent 的物理位置
- **地理位置详情**：展示每个 Agent 的公网 IP、地区、ISP 运营商、ASN 信息

### 3.2 设备列表（Agents）

管理所有已上线的 Agent，支持详细查看与操作：

- **Agent 列表**：主机名、IP 地址、操作系统、在线状态、最后心跳时间
- **在线/离线筛选**：快速过滤不同状态的 Agent
- **设备详情面板**：
  - 基本信息：主机名、用户名、进程名、PID、架构、是否提权
  - 首次上线时间、心跳间隔
  - **硬件指纹**：CPU（型号/核心数）、GPU（型号/驱动/显存）、RAM 总容量、磁盘（型号/容量/类型/序列号）、显示器（分辨率）、主板厂商、BIOS 版本
  - **网络信息**：公网 IP、地区、ISP、ASN、经纬度
- **凭据抓取**：对单个 Agent 一键下发内存凭据抓取命令（Mimikatz）

### 3.3 远程终端（Shell）

提供完整的交互式远程终端体验，超越传统的单行命令执行：

- **全功能伪终端（PTY）**：
  - Linux：分配完整 PTY，支持 vim、top、nano、mysql 等交互式命令，Tab 补全、方向键、历史命令回溯
  - Windows：具有完整执行上下文的交互式 PowerShell 会话，支持内存加载自定义模块
- **WebSocket 实时传输**：命令输出毫秒级同步到控制台
- **xterm.js 渲染**：支持 ANSI 颜色、快捷键、终端尺寸自适应
- **读写/只读模式**：可选择仅监控或完整控制

### 3.4 屏幕监控（Screen Monitor）

静默监控目标设备的屏幕画面：

- **定时截图**：支持 1/3/5/10/15 FPS 多档帧率
- **画质调节**：原始/1080p/720p/540p/360p/240p 多档分辨率
- **图像差分压缩**：仅传输画面变化的像素区块，大幅降低带宽消耗
- **底层 API 捕获**：通过 DirectShow/Media Foundation 接口读取数据，规避部分 EDR 的 Hook 检测

### 3.5 系统媒体（Media Monitor）

针对配备外设的终端，提供物理环境侦察：

- **摄像头监控**：
  - 静默抓拍或低帧率视频传输
  - 通过底层接口直接读取数据流，尽量规避系统"摄像头使用中"提示
  - 支持 5/10/15/20/30 FPS 多档帧率
  - 自动检测可用摄像头设备列表
- **麦克风监听**：
  - 实时音频流传输
  - 自动检测可用麦克风设备列表

### 3.6 文件浏览（File Manager）

完整的远程文件系统管理能力：

- **驱动器浏览**：自动枚举所有磁盘分区（Windows 盘符 / Linux 挂载点）
- **目录导航**：前进/后退/上级目录，支持路径面包屑导航
- **文件操作**：上传、下载、重命名、移动、复制、删除
- **压缩/解压**：支持创建压缩包和在线解压
- **创建快捷方式**：Windows 快捷方式创建
- **大文件分块传输**：支持断点续传，避免网络波动导致失败，控制瞬时带宽防止触发流量阈值告警
- **反取证时间戳伪造（Timestomping）**：支持将文件 MAC 时间修改为与同目录文件一致，抹除操作痕迹

### 3.7 软件数据（Software Data）

从目标设备提取第三方软件的账号信息和敏感数据：

#### 3.7.1 微信（WeChat）

- 自动定位微信安装目录和数据文件夹
- 枚举所有登录过的微信号（wxid）
- 浏览每个账号的数据文件夹（月份文件夹、文件列表）
- 支持下载指定文件到控制端

#### 3.7.2 QQ

- 自动定位 QQ 安装目录和登录数据
- 枚举所有登录过的 QQ 号码
- **QQ 头像与昵称查询**：通过服务端代理调用 QQ 空间 API，获取 QQ 号码对应的头像和昵称，规避浏览器跨域限制
- 浏览每个账号的数据文件

#### 3.7.3 浏览器（Browser）

- **支持的浏览器**：Chrome（Chromium 系）、Edge、Firefox
- **密码提取**：解密并提取浏览器保存的登录密码（含网站 URL、用户名、明文密码）
- **Cookie 提取**：提取所有网站的 Cookie（域名、名称、值、过期时间）
- **浏览历史**：提取浏览历史记录（标题、URL、访问次数、最后访问时间），按域名分组展示
- **加密解密**：
  - Chromium 系：DPAPI 解密 AES Master Key → AES-256-GCM 解密密码
  - Firefox：key4.db 提取 Master Key → PBES2 + 3DES/AES-GCM 解密
- **注意事项**：
  - Chrome v20+ 的 App-Bound Encryption 需要管理员权限才能解密
  - Native AOT 编译版本存在已知限制（详见项目 README 已知问题章节）

#### 3.7.4 AI 密钥扫描（AI）

- **扫描目标**：检测 6 款主流 AI 编码工具本地存储的 API 密钥
  - **Claude Code**（Anthropic）：`~/.claude/settings.json` 的 `env` 配置块 + 环境变量
  - **OpenCode**：`~/.config/opencode/opencode.json` + `auth.json` 认证文件 + 环境变量
  - **CodeX**（OpenAI CLI）：`~/.codex/config.toml` + 环境变量
  - **Gemini**（Google）：`%APPDATA%/gemini/settings.json` + `~/.gemini/.env` + 环境变量
  - **OpenClaw**：`~/.openclaw/openclaw.json` + 各 Agent 的 `auth-profiles.json` + 环境变量
  - **HermesAgent**（Nous Research）：`~/.hermes/.env` + 环境变量
- **智能过滤**：仅提取字段名包含 API/KEY/TOKEN/BASEURL/URL 等关键词的配置项
- **显示模式**：默认脱敏显示（前4位+后4位），支持一键切换明文/脱敏
- **来源标注**：区分配置文件提取和环境变量提取

### 3.8 反向代理（Proxy Browser）

利用目标设备的网络环境访问网页，实现内网穿透浏览：

- **多标签页浏览**：支持新建/关闭标签页
- **URL 导航**：输入 URL 通过 Agent 发出 HTTP 请求
- **页面操作**：前进/后退/刷新
- **代理效果**：所有 Web 请求以 Agent 的 IP 和网络环境发出，可用于访问内网 Web 应用或绕过 IP 限制

### 3.9 压力测试（Stress Test）

分布式多点 DDoS 模拟攻击平台，用于测试目标系统的抗压能力：

- **攻击节点选择**：从在线 Agent 列表中选择参与攻击的节点
- **攻击目标配置**：指定目标 IP/域名 + 端口
- **八种攻击方式**：

| 层级 | 攻击方式 | 说明 |
|------|---------|------|
| 四层（网络层） | SYN Flood | 原始套接字发送大量 SYN 包，耗尽连接队列 |
| 四层（网络层） | UDP Flood | 大量 UDP 数据包轰炸目标端口 |
| 四层（网络层） | ICMP Flood | 大量 ICMP Echo 请求占用目标带宽 |
| 四层（网络层） | 反射放大（Reflection） | 利用 DNS/NTP 服务器伪造源 IP 进行流量放大 |
| 七层（应用层） | HTTP Flood | HTTP 客户端池并发 GET/POST 请求 |
| 七层（应用层） | Slowloris | 慢速 HTTP 头发送，占用连接数 |
| 七层（应用层） | TCP 连接耗尽 | 大量 TCP 连接不释放 |
| 七层（应用层） | 协议畸形包 | 畸形 TCP SYN+Payload / TLS 畸形握手 |

- **攻击参数**：每节点线程数、数据包大小、攻击时长（秒，0=无限）
- **关闭后持续**：关闭页面后攻击持续执行直到手动停止或超时
- **隐蔽措施**：随机 User-Agent、请求间隔抖动、随机载荷、源端口随机化、TCP Window Size 随机
- **实时监控**：
  - 流量图表（ECharts 实时曲线，每个 Agent 一条折线 + 总计）
  - 状态面板（运行时长、总流量、总包数、活跃连接数、每个 Agent 的当前速率）
- **历史记录**：所有攻击任务写入数据库，支持历史查询

### 3.10 系统信息（System Info）

全面收集目标主机的系统状态信息：

#### 3.10.1 进程管理

- 列出所有运行中的进程（PID、名称、CPU 时间、内存占用、线程数）
- 进程名称/PID 筛选搜索
- **结束进程**：远程终止指定进程
- 数据哈希比对：仅变更时传输完整数据，节省带宽

#### 3.10.2 窗口管理（Windows Only）

- 枚举所有可见窗口（句柄、标题、进程名、窗口类名）
- **窗口操作**：置顶、置底、最小化、最大化
- **修改窗口标题**：远程修改任意窗口的标题文字

#### 3.10.3 环境变量

- 分别展示系统级和用户级环境变量
- **在线编辑**：添加、修改、删除环境变量

#### 3.10.4 网络信息

- **网络接口**：所有网卡信息（名称、类型、MAC 地址、速率、IPv4/IPv6 地址）
- **公网信息（WAN）**：公网 IP、网关、地区、ISP、ASN、经纬度
- **Wi-Fi 密码**：已保存的 Wi-Fi 配置（SSID + 明文密码）
- **附近 Wi-Fi**：扫描周边 Wi-Fi 热点（SSID、BSSID、加密类型、信号强度）
- **代理设置**：系统代理配置（启用状态、服务器、端口、绕过列表）
- **DNS 后缀**

#### 3.10.5 局域网扫描

- 自动探测目标主机所在子网
- ARP/NetBIOS 扫描局域网内其他设备（IP、MAC、主机名、来源）

#### 3.10.6 本地账户

- 枚举系统本地用户账户（账户名、全名、描述、SID、启用状态、管理员权限）
- 显示账户安全属性（密码要求、最后登录、过期时间、密码最后修改时间等）
- **凭据抓取**：一键执行内存凭据抓取（Mimikatz），提取 LSASS 进程中的明文密码和哈希值

### 3.11 构建荷载（Builder）

自助式 Agent 生成工具，允许操作员定制和生成 Agent 可执行文件：

- **平台选择**：Windows / Linux
- **应用类型**：可执行文件（.exe / ELF）
- **连接配置**：指定回连的 Server 地址和端口
- **构建选项**：
  - **裁剪未使用代码**：移除未引用的程序集以减小体积
  - **Obfuscar 混淆**：对编译后的程序集进行名称混淆
  - **注入垃圾数据**：在可执行文件末尾追加随机数据（可配置大小 MB），改变文件哈希特征
- **元数据伪装**：自定义文件属性（公司名称、文件描述、产品名称、版权信息、文件版本）
- **自定义图标**：上传或输入 URL 指定自定义图标
- **行为开关**：
  - 需要管理员权限运行
  - 复制到 AppData 目录
  - 启用持久化（开机自启）
- **构建记录**：保存所有构建历史，支持查看配置详情和下载

### 3.12 审计日志（Audit Logs）

完整的操作审计追溯体系：

- **记录内容**：时间、操作用户、操作动作、目标 Agent IP、操作者 IP、执行结果（成功/失败）
- **筛选搜索**：按用户、操作、IP、Agent ID 进行全文搜索
- **分页浏览**：支持大日志量下的分页导航
- **不可删除**：前端不提供日志删除功能，确保赛后复盘绝对客观

---

## 四、功能矩阵总览

| 功能模块 | 描述 | Agent 端 | 控制端 |
|---------|------|---------|--------|
| 仪表盘 | 全局态势总览、流量趋势、设备分布地图 | — | ✓ |
| 设备列表 | Agent 管理、硬件指纹、状态监控 | ✓ | ✓ |
| 远程终端 | 交互式 PTY/PowerShell 终端 | ✓ | ✓ |
| 屏幕监控 | 静默截图/低帧率监控 | ✓ | ✓ |
| 系统媒体 | 摄像头抓拍 + 麦克风监听 | ✓ | ✓ |
| 文件浏览 | 远程文件管理、上传下载、时间戳伪造 | ✓ | ✓ |
| 微信数据 | 微信号枚举、数据文件浏览下载 | ✓ | ✓ |
| QQ 数据 | QQ 号枚举、头像昵称查询 | ✓ | ✓ |
| 浏览器凭据 | Chrome/Edge/Firefox 密码/Cookie/历史 | ✓ | ✓ |
| AI 密钥 | 6 款 AI 工具 API 密钥扫描 | ✓ | ✓ |
| 反向代理 | 通过 Agent 代理浏览网页 | ✓ | ✓ |
| 压力测试 | 分布式 DDoS 8 种攻击方式 | ✓ | ✓ |
| 进程管理 | 进程列表、筛选、结束进程 | ✓ | ✓ |
| 窗口管理 | 窗口枚举、置顶/置底/最小化/改标题 | ✓ | ✓ |
| 环境变量 | 系统/用户变量查看与编辑 | ✓ | ✓ |
| 网络信息 | 网卡、WAN、Wi-Fi 密码、附近热点、代理 | ✓ | ✓ |
| 局域网扫描 | ARP/NetBIOS 内网设备发现 | ✓ | ✓ |
| 本地账户 | 系统账户枚举、凭据抓取 | ✓ | ✓ |
| 构建荷载 | 自助式 Agent 生成、混淆、元数据伪装 | 服务端构建 | ✓ |
| 审计日志 | 全量操作审计、不可删除 | 服务端记录 | ✓ |

---

## 五、产品价值

### 5.1 对红队的价值

- **全链条覆盖**：从初始植入、持久化、权限提升、凭据窃取、横向移动到数据回传，提供完整的后渗透工具链，无需切换多款工具
- **高隐蔽性**：内存驻留、流量伪装、反沙盒检测、Native AOT 编译，最大限度规避 EDR/AV 检测
- **跨平台作战**：统一的操作界面同时管理 Windows 和 Linux 肉鸡，降低操作复杂度
- **多人协同**：团队多人可同时登录控制台，命令下发毫秒级同步，避免任务冲突
- **一键部署**：自助式构建荷载功能，修改配置后即刻生成定制化 Agent

### 5.2 对蓝队的价值

- **真实攻击模拟**：提供与真实 APT 攻击行为一致的 TTPs（战术、技术、程序），用于检验防御体系有效性
- **全面审计追溯**：所有操作不可删除地记录在案，赛后复盘提供客观完整的数据支撑
- **压力测试**：分布式 DDoS 模拟可用于验证 WAF、DDoS 清洗设备的实际防护能力
- **安全意识提升**：直观展示各类数据窃取的危害性，提升团队对凭据管理、终端安全的重视

### 5.3 对安全研究者的价值

- **开源透明**：完整源代码开放，可审计、可修改、可二次开发
- **现代化技术栈**：.NET 10 Native AOT、React + HeroUI、MongoDB，代表当前攻防工具开发的技术前沿
- **模块化架构**：Agent 采用接口化设计，新增后渗透模块只需实现标准接口，降低开发门槛
- **研究平台**：可用于 EDR 绕过技术研究、C2 通信特征分析、内存取证对抗等安全研究课题

---

## 六、技术规格

| 项目 | 规格 |
|------|------|
| **Agent 运行时** | .NET 10 Native AOT |
| **Agent 体积** | 基础核心 ~1-2 MB |
| **Agent 支持平台** | Windows 10/11、Linux (x64/ARM64) |
| **Server 运行时** | ASP.NET Core 10 (Kestrel) |
| **数据库** | MongoDB |
| **控制台技术栈** | React 19 + Vite + HeroUI + TailwindCSS |
| **实时通信** | WebSocket（双向低延迟） |
| **Agent-Server 加密** | AES-256-GCM + RSA 密钥协商 |
| **控制台认证** | JWT (RS256) + RBAC |
| **浏览器兼容** | Chrome / Edge / Firefox 最新版本 |
| **开源协议** | GNU General Public License v3.0 |

---

## 七、法律声明

Libra-Nextgen 是一款仅供**授权红蓝对抗演练、渗透测试、安全研究及教育目的**使用的工具。

- **禁止**在未获得目标系统所有者明确书面授权的情况下使用本软件
- **禁止**将本软件用于任何违反中华人民共和国法律法规或国际公约的活动
- 使用者须自行承担因不当使用本软件所产生的一切法律责任和后果
- 本软件的开发者及贡献者**不承担**任何因使用者滥用本软件而导致的直接或间接损失

> **君子协定：** 此工具为正义而构建。请在道德和法律允许的框架内使用它。
